version: '3.7'
services:
  app:
    container_name: ghcr.io/ixofoundation/ixo-feegrant-nest:latest
    build:
      context: .
      dockerfile: Dockerfile
    tty: true
    image: ixo-feegrant-nest
    restart: unless-stopped
    volumes:
      - ./:/app
    ports:
      - 3000:3000
